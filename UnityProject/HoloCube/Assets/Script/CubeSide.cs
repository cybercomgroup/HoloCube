/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CubeSide : MonoBehaviour
{
    private Color color;
    private List<Color> colorList;
    private int id;
    private List<GameObject> Cubies; 

    // Use this for initialization
    void Start()
    {
        for (int i = 0; i < 8; i++)
        {
            colorList.Add(Color.black);
        }

        foreach 
    }

    public AddColorToList<>

    public void DrawColor(int id, Color c)
    {
        var obj = GameObject.Find("Plane.065");
        var colorRender = obj.GetComponent<Renderer>();
        colorRender.enabled = true;
        colorRender.material.color = new Color(1, 0.5f, 0);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
*/